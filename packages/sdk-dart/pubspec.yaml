name: tinyhumans_sdk
description: Dart SDK for the TinyHumans Memory API.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
